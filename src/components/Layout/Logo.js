import React from 'react';
import logo from '../../img/logo.png';

const Logo = () => (
  <div
    style={{
      width: '100%',
      padding: '1rem',
      textAlign: 'center',
    }}>
    <img src={logo} alt="cra-rr4-redux-antd" />
  </div>
);

export default Logo;
